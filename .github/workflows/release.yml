name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-13, macos-14]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build with PyInstaller (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        pyinstaller --noconsole --onefile --name "LittleTyrantInstaller_Win" main.py

    - name: Build with PyInstaller (macOS Intel)
      if: matrix.os == 'macos-13'
      run: |
        pyinstaller --noconsole --onefile --name "LittleTyrantInstaller_Mac_Intel" main.py

    - name: Build with PyInstaller (macOS Apple Silicon)
      if: matrix.os == 'macos-14'
      run: |
        pyinstaller --noconsole --onefile --name "LittleTyrantInstaller_Mac_Silicon" main.py

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: binary-${{ matrix.os }}
        path: dist/LittleTyrantInstaller_*

  release:
    needs: build
    runs-on: windows-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: artifacts/**/LittleTyrantInstaller_*
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
